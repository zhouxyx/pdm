spring:
  application:
    name: pdm-center
server:
  port: 8761

eureka:
  instance:
    hostname: registry
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl: 
#      defaultZone: http://root:root@127.0.0.1:${server.port}/eureka/
      defaultZone: http://localhost:8761/eureka/
      
#security:
#  basic:
#    enabled: true
#  user:
#    name: root
#    password: root      
    
logging:
  level:
    root: INFO    